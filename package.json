{
  "name": "app",
  "private": "true",
  "dependencies": {
    "@hotwired/stimulus": "^3.1.1",
    "@hotwired/turbo-rails": "^7.2.4",
    "@instructure/ui": "^8.30.0",
    "esbuild": "^0.15.12",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "remount": "^1.0.0"
  },
  "scripts": {
    "build": "esbuild app/javascript/*.* --bundle --sourcemap --outdir=app/assets/builds --public-path=assets"
  }
}
